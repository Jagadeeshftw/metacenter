[project]
name = "metacenter-fork"
description = ""
authors = []
telemetry = false
cache_dir = "./.cache"

[contracts.risk-feed-trait]
path = "contracts/risk-feed-trait.clar"
epoch = "4.0"

[contracts.pox5-reader]
path = "contracts/pox5-reader.clar"
epoch = "4.0"

[contracts.risk-feed]
path = "contracts/risk-feed.clar"
epoch = "4.0"

[contracts.coverage-guard]
path = "contracts/coverage-guard.clar"
epoch = "4.0"

# Check-checker settings:
# trusted_sender: if true, inputs are trusted after tx_sender has been checked.
# trusted_caller: if true, inputs are trusted after contract-caller has been checked.
# callee_filter: if true, untrusted data may be passed into a private function without a
# warning, if it gets checked inside. This check will also propagate up to the
# caller.
# More informations: https://www.hiro.so/blog/new-safety-checks-in-clarinet

[repl.analysis]
passes = ["check_checker"]
check_checker = { trusted_sender = false, trusted_caller = false, callee_filter = false }

# Mainnet fork pinned at Stacks block 9,019,000 (Bitcoin block 967,574): after the
# cycle-143 first distribution (calculation-height 967,399), before the next one.
[repl.remote_data]
enabled = true
api_url = 'https://api.hiro.so'
initial_height = 9019000
use_mainnet_wallets = true
